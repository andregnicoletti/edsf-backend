export type CreateLeadType = {
    data: {
        name: string,
        company: string,
        phone: string,
        email: string,
        referralSource: string,
        message: string,
    }
}